.. _usage:

Installation
=====

- enable ``'django.template.loaders.eggs.Loader'`` in ``TEMPLATE_LOADERS`` in your ``settings.py`` file
- add ``django_siteanalytics`` to ``INSTALLED_APPS`` in your ``settings.py`` file
